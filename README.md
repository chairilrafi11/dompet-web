# dompet-web

Frontend untuk aplikasi manajemen dompet. React + Vite + TypeScript.

## Menjalankan

```bash
npm install
npm run dev   # http://localhost:8021 (proxy /api -> 8020)
```

Pastikan `dompet-api` berjalan di port 8020 terlebih dahulu.

## Test

```bash
npm test
```
